# ✅ 最終決定: Sora 2 Web UI (完全無料)

## 📊 Sora 2 Free Tier詳細

### 制限事項
- **1日30本まで**
- **5秒 または 10秒** (情報源により異なる)
- **720p解像度**
- **ウォーターマークあり** (可能性)

### 月間生成可能数
- **1日30本 × 30日 = 900本/月**
- **完全無料 ($0)**

---

## 🎯 ユーザーの回答

### Q1: sora.com にアクセスできますか?
**回答**: YES ✅

### Q2: 8秒動画は必須ですか?
**回答**: NO ✅
- **→ 5秒または10秒でOK**

---

## ✅ 決定: プランA (Sora Web UI 完全無料)

### 実装方針

#### 1. **プロンプト自動生成機能**
- ユーザーがUIで設定入力
- システムが自動的にSora用プロンプト生成
- **クリップボードにワンクリックコピー**

#### 2. **Sora Web UI連携**
- プロンプトコピー後、**新しいタブでsora.com自動オープン**
- ユーザーが貼り付けて動画生成
- 動画ダウンロード

#### 3. **YouTube自動投稿**
- ダウンロードした動画をシステムにアップロード
- YouTube Data API v3で自動投稿
- タイトル・説明文・タグは自動生成済み

---

## 📋 実装プラン

### Phase 1: プロンプト生成機能 (30分)
```javascript
// frontend: public/static/app.js

function generateSoraPrompt() {
  const character = getCharacterSettings();
  const video = getVideoSettings();
  const music = getMusicSettings();
  
  let prompt = '';
  
  // キャラクター
  if (character.mode === 'prompt') {
    prompt += character.prompt + ', ';
  }
  
  // アクション
  prompt += `${video.action} `;
  
  // 楽器
  if (video.instrument) {
    prompt += `${video.instrument}, `;
  }
  
  // テーマ・ジャンル
  prompt += `${video.theme} theme, ${music.genre} music style, `;
  
  // 品質
  prompt += 'professional music video, cinematic lighting';
  
  return prompt;
}

// クリップボードコピー
function copyToClipboard(text) {
  navigator.clipboard.writeText(text);
  showNotification('✅ プロンプトをクリップボードにコピーしました！');
}

// Sora Web UI オープン
function openSoraWebUI() {
  window.open('https://sora.com', '_blank');
}
```

### Phase 2: YouTube自動投稿 (1時間)
```javascript
// backend: server.js

import { google } from 'googleapis';

const youtube = google.youtube({
  version: 'v3',
  auth: oauth2Client
});

// 動画アップロード
async function uploadToYouTube(videoPath, metadata) {
  const res = await youtube.videos.insert({
    part: 'snippet,status',
    requestBody: {
      snippet: {
        title: metadata.title,
        description: metadata.description,
        tags: metadata.tags.split(', ')
      },
      status: {
        privacyStatus: 'public'
      }
    },
    media: {
      body: fs.createReadStream(videoPath)
    }
  });
  
  return res.data;
}
```

### Phase 3: スケジューラ設定 (30分)
- 1日3本の投稿スケジュール
- 09:00, 15:00, 21:00
- 外部Cron (cron-job.org) で自動実行

---

## 💰 最終コスト

| 項目 | 月間コスト |
|------|-----------|
| Sora 2 Web UI | **$0** |
| YouTube Data API | $0 |
| Cloudflare Pages | $0 |
| Cloudflare D1 | $0 |
| 外部Cron | $0 |
| **合計** | **$0/月** |

---

## 🚀 実装スケジュール

### 今日 (2-3時間)
1. ✅ プロンプト生成機能 (30分)
2. ✅ クリップボードコピー (10分)
3. ✅ Sora Web UI連携 (10分)
4. 🔜 YouTube OAuth設定 (30分)
5. 🔜 YouTube投稿機能 (1時間)
6. 🔜 E2Eテスト (30分)

### 明日
1. Cloudflare Pages デプロイ
2. 外部Cron設定
3. 本番運用開始

---

## 📝 ワークフロー

### ユーザーの操作フロー
```
1. UIで設定入力 (30秒)
   ├─ キャラクター設定
   ├─ 動画内容設定
   └─ 音楽設定

2. 「プロンプト生成」ボタンをクリック
   → プロンプトが自動生成される

3. 「Soraで生成」ボタンをクリック
   ├─ プロンプトがクリップボードにコピー
   └─ 新しいタブでsora.com開く

4. Sora Web UIで貼り付け → 生成 (1-2分)
   → 動画ダウンロード

5. システムに戻って「動画アップロード」
   → YouTube自動投稿 ✅
```

### 自動化部分
- ✅ プロンプト生成
- ✅ YouTube投稿
- ✅ タイトル・説明文・タグ生成
- ✅ 履歴保存

### 手動部分
- ⚠️ Sora Web UIで動画生成 (1-2分)
- ⚠️ 動画ダウンロード (10秒)

---

## 🎯 次のステップ

**今すぐ実装を開始しますか?**

**A) 今すぐ実装開始** (推奨)
1. プロンプト生成機能
2. YouTube OAuth設定
3. YouTube投稿機能

**B) YouTube OAuth設定方法を先に確認**
- Google Cloud Console
- OAuth 2.0設定
- 認証フロー

**C) もう少し詳しく説明してほしい**

どれにしますか?
